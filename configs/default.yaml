system:
  seed: 42
  num_workers: 8
  mixed_precision: true

training:
  epochs: 50
  batch_size: 32
  accumulate_steps: 4
  save_every: 2

optimizer:
  type: adam
  lr: 0.0001
  weight_decay: 0.00001

logging:
  tensorboard: true
  log_dir: logs/

distributed:
  backend: nccl
